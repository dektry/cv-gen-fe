export enum CandidatesSelectFilter {
  SOFT = 'waiting for softskills interview',
  TECH = 'waiting for technical interview',
  ALL = 'all candidates',
}

export const defaultPageSize = 10;
export const defaultCurrentPage = 1;
